# Session runtime against a Flink session cluster on THIS machine.
# Bring the cluster up with:
#   bash examples-bin/run-session-cluster.sh
# Then:
#   cp .env.cluster.local.example .env
#   /Users/bengamble/Agentic-Flink/.venv/bin/jupyter notebook notebooks/09_session_cluster_levels.ipynb

AGENTIC_FLINK_MODE=session
FLINK_REST_URL=http://localhost:8081
FLUSS_BOOTSTRAP=localhost:9123

ANTHROPIC_API_KEY=

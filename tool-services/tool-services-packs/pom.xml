<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.jagentic</groupId>
    <artifactId>agentic-streaming-reactor</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <relativePath>../../reactor/pom.xml</relativePath>
  </parent>

  <artifactId>tool-services-packs</artifactId>
  <name>Agentic Tool Services — Packs</name>
  <description>Flink-free library of self-describing tool packs (web/Tika, RAG, inference, utility)
    that register into a jagentic-core ToolRegistry and can be served over MCP/REST/gRPC/pub-sub.</description>

  <!--
  Module of reactor/pom.xml. On its own, build after installing jagentic-core:
      ./mvnw -q -f ports/jagentic-core/pom.xml install -DskipTests
      ./mvnw -f tool-services/tool-services-packs/pom.xml install
  -->
  <properties>
    <jackson.version>2.17.2</jackson.version>
    <tika.version>2.9.2</tika.version>
    <jsoup.version>1.18.1</jsoup.version>
  </properties>

  <dependencies>
    <!-- The Flink-free core: ToolRegistry, ToolServer, Embedder/VectorStore/inference SPIs. -->
    <dependency>
      <groupId>org.jagentic</groupId>
      <artifactId>jagentic-core</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- @Tool / @P annotations for the utility pack's annotated tools. -->
    <dependency>
      <groupId>dev.langchain4j</groupId>
      <artifactId>langchain4j-core</artifactId>
      <version>${langchain4j.version}</version>
    </dependency>

    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>

    <!-- Web + document pack deps: HTML parsing (Jsoup), document extraction (Tika), robots.txt
         (crawler-commons). Compile-scope so anyone depending on this library gets a working
         web pack. -->
    <dependency>
      <groupId>org.jsoup</groupId>
      <artifactId>jsoup</artifactId>
      <version>${jsoup.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.tika</groupId>
      <artifactId>tika-core</artifactId>
      <version>${tika.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.tika</groupId>
      <artifactId>tika-parsers-standard-package</artifactId>
      <version>${tika.version}</version>
    </dependency>
    <dependency>
      <groupId>com.github.crawler-commons</groupId>
      <artifactId>crawler-commons</artifactId>
      <version>1.4</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <!-- emit parameter names so the @Tool reflection binder can read them -->
          <parameters>true</parameters>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>

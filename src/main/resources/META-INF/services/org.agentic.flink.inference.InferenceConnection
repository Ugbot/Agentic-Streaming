# InferenceConnection providers are registered by backends.
# - DJL backend registers org.agentic.flink.inference.djl.DjlInferenceConnection
# - ONNX, DL4J, etc. would register here too.
# This file is intentionally empty in the base build; the DJL deps are optional.

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.jagentic.ports</groupId>
  <artifactId>agentic-temporal</artifactId>
  <version>0.1.0</version>
  <packaging>jar</packaging>
  <name>agentic-temporal</name>
  <description>Agentic-Flink essence on Temporal — a durable entity workflow per
    conversation (workflowId = conversationId = single-writer + durable, event-sourced
    state), reusing the Flink-free jagentic-core.</description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <temporal.version>1.29.0</temporal.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.jagentic</groupId>
      <artifactId>jagentic-core</artifactId>
      <version>0.1.0</version>
    </dependency>

    <!-- Durable execution: workflows + activities. workflowId per conversation gives
         single-writer + durable, event-sourced state (C1+C2+C3). -->
    <dependency>
      <groupId>io.temporal</groupId>
      <artifactId>temporal-sdk</artifactId>
      <version>${temporal.version}</version>
    </dependency>

    <!-- In-memory time-skipping test service: runs workflows with NO external Temporal
         server, the analogue of Pekko's LocalDemo / Pulsar's InMemoryContext. Used by
         both LocalDemo (compile scope) and the tests. -->
    <dependency>
      <groupId>io.temporal</groupId>
      <artifactId>temporal-testing</artifactId>
      <version>${temporal.version}</version>
    </dependency>

    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>2.0.13</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.2.0</version>
        <configuration>
          <mainClass>org.jagentic.ports.temporal.LocalDemo</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>

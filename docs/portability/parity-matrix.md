# Parity matrix: what each version of Agentic Streaming can do, per backend

> **Proven capabilities live in [`../capabilities.md`](../capabilities.md)**, generated by
> `spec/tools/conformance_matrix.py` from a run of the 22 shared agentic/v1 conformance fixtures
> against each runtime's own binding. That table is the only place a runtime may be called
> `supported` for an agentic/v1 capability; every cell there is `supported`, `partial`,
> `unsupported` or `not_tested` and traces to a fixture outcome. The tables below describe
> feature *coverage and delivery model* per core/backend and are maintained by hand. Where
> this page and `capabilities.md` disagree, `capabilities.md` is right and this page is stale.
>
> Nine bindings run the fixtures today: reference, jvm-core, flink, pekko, clojure, python,
> pyflink, and the two Python facade bindings (python-jvm, python-flink). Every other backend
> on this page is an experimental adapter: it runs the banking example on its engine and is
> not conformance tested. The "Conformance tested" column below says which is which.

Agentic Streaming is **one agent essence** with many runtimes. The agent features live in
three shared, Flink-free cores (`pyagentic`, `jagentic-core`, `goagentic`); each engine
adapter is a thin, **injectable** seam over a core. So the *agent's capabilities* are the
same everywhere, what differs per backend is the **delivery model** (online turn vs
streamed vs batch) and where **durability / ordering** come from. Nothing agentic is
locked to Flink.

(Two groupings, don't conflate them: the **three cores** above are *where the features live*;
the **three first-class runtimes** are Flink,**Pekko** [core-backed actors] and **Clojure**
[a pure-Clojure reimplementation of the essence], each a polished, batteries-included deployment.)

## 1. The three cores are at parity

The cores are behaviorally identical on the banking goldens (enforced by cross-core parity
tests: `test_parity.py` / `ParityTest` / `parity_test.go`). "yes" in this table means the
feature is implemented in that core with its own unit tests; it does not mean the feature is
proven by a shared conformance fixture. Only `pyagentic` and `jagentic-core` run the fixtures;
`goagentic` does not, so nothing in this table makes a claim about Go under agentic/v1. Several
rows below (`context_window`, `timers`, `cep`, `event_time`) are `unsupported` for every
non-reference binding in [`../capabilities.md`](../capabilities.md) even though the cores
implement them: the fixture-facing binding does not wire them yet.

| Capability | pyagentic (Python) | jagentic-core (Java) | goagentic (Go) |
|------------|:---:|:---:|:---:|
| Event / ChatMessage / TurnResult | yes | yes | yes |
| ConversationStore (multi-turn memory) | yes | yes | yes |
| KeyedStateStore | yes | yes | yes |
| ToolRegistry + tool execution | yes | yes | yes |
| RoutedGraph (router → path → verifier) | yes | yes | yes |
| Hot+cold retrieval (FNV‑1a embedder, identical) | yes | yes | yes |
| **LLM brain SPI** (ChatClient + ReAct loop) | yes | yes | yes |
| Providers (unified lib + native): litellm/Ollama/OpenAI · langchaingo · LangChain4J | yes | yes | yes |
| **Embedder SPI** (hashing default; real via the LLM lib) | yes | yes | yes |
| **Structured output** (schema‑validated answers) | yes | yes | yes |
| **Skills** (tools + prompt fragment + facts) | yes | yes | yes |
| **Guardrails**, regex + **classifier** (lexicon/embedding) | yes | yes | yes |
| **Listeners** (≈9 lifecycle hooks + metrics + composite) | yes | yes | yes |
| **Vector store SPI**, in‑memory · **in‑process HNSW** · Qdrant | yes | yes | yes |
| Embeddable analytical store (DuckDB) | yes |, |, |
| **Long‑term store SPI**, in‑memory · Postgres | yes | yes | yes |
| Hot‑swap conversation store, in‑memory · **Redis/Valkey** | yes | yes | yes |
| **MCP client** (register MCP server tools) | yes, official SDK | yes, pure‑Java | yes, mark3labs |
| **A2A client** (peer‑as‑tool, card+send+retries) | yes | yes | yes |
| **Saga / compensation** (reverse‑order rollback) | yes | yes | yes |
| **Context‑window mgmt** (MoSCoW compaction) | yes | yes | yes |
| **Web toolkit** (robots‑aware fetch + extract) | yes | yes | yes |
| **DL inference SPI** (Classifier/Scorer) | yes | yes | yes |
| **GraphBuilder** (declarative spec → graph) | yes | yes | yes |
| **YAML pipeline loader** (full Phase‑F schema) | yes | yes | yes |
| **EventStream substrate** (Channel + StreamRuntime, ordered per‑key) | yes | yes | yes |
| **Timer service** (logical time; durable via the store SPI) | yes | yes | yes |
| **Windows** (sliding / tumbling / session + aggregates) | yes | yes | yes |
| **Complex event processing**, portable NFA matcher (begin/next/followedBy/within) | yes | yes | yes |
| **Replay / time‑travel** (event‑log replay + replay‑until / as‑of) | yes | yes | yes |
| **Human‑in‑the‑loop** (suspend / resume / timeout‑escalate) | yes | yes | yes |
| **Tracing SPI** (no‑op default · recording · OTel opt‑in) | yes | yes | yes |

The last seven rows are the **stream-stateful core** (`stream` · `timers` · `windows` · `cep` ·
`replay` · `suspend` · `trace`), they also ship in the **Clojure** core (`agentic-clj`) at the same
cross-core parity, so **CEP, timers and windows are no longer Flink-only**: the same "3 anomalies in
5 minutes → incident" pattern runs on every core. See
[`stream-stateful-core.md`](stream-stateful-core.md).

All three cores ship the same SPIs **and** one working reference implementation per heavy
integration (Qdrant vectors, Postgres long-term, Redis/Valkey conversations, the official
MCP client, HTTP A2A). Other backends are opt-in: implement the SPI + add a `kind` to the
loader factory. Real LLM/embedding providers, external stores and DL models are import-/
dependency-guarded so the model-free defaults (Stub chat, FNV hashing embedder, in-memory
stores, lexicon classifier) keep the offline test suites green; live paths are verified
against podman infra (Ollama, Qdrant, Postgres, Valkey) and skip cleanly when absent.

## 2. Backends: capability + limitations

Because adapters are injectable and features live in the cores, every backend that
executes the graph runs the agent logic of its core (LLM brain, tools, retrieval,
guardrails, listeners, multi-turn memory, agent-calls). That is a statement about where the
code lives, not a test result. Which of those a given runtime has actually *proven* against
the shared fixtures is [`../capabilities.md`](../capabilities.md); the columns below capture
the per-backend differences in delivery model, and the caveats that used to sit in footnotes
are now in the row they apply to.

Legend. Delivery: **online** (synchronous turn) · **streamed** (keyed stream) · **batch**
(offline). C2 = single-writer-per-conversation, C3 = durable/fault-tolerant. "YAML loader"
says how a `pipeline.yaml` reaches the engine: **runner** (a one-command entry point takes
the file), **engine Runtime** (the JVM `GraphBuilder.Built` is constructed by the engine
module's own entry point; the core loader ships only `local`), or **core build** (the
GraphBuilder builds the graph and the engine hosts it through its own `configure(...)` or
constructor seam, not a one-line loader backend).

| Backend | Lang | Conformance tested | Delivery | C2 (ordering) | C3 (durability) | YAML loader | Verified here | Caveat |
|---------|:----:|:------------------:|:--------:|---------------|-----------------|:-----------:|---------------|--------|
| **Flink** (first‑class) | JVM/Py | yes: `flink`, `pyflink`, `python-flink` | streamed | native keyBy | native checkpoints | runner (`FlinkPipelineRunner`) | `FlinkConformanceTest`, `FlinkPipelineRunnerTest`, `WorkflowTurnFunctionMiniClusterTest` | The YAML runner keeps per-conversation memory in the portable in-memory store, not in Flink checkpointed state; only the `WorkflowTurnFunction` path is savepoint tested. The runner has no working standalone `exec:java` command from a fresh clone (see the README quick start) |
| **Pekko** (first‑class) | JVM | yes: `pekko` | online/actor | actor mailbox | Persistence | runner (`PipelineMain`) | `PekkoConformanceTest`, `PekkoBackendPipelineTest`, `ConversationEntityTest`, `RedisJournalIT` | Durability across a system restart is tested on the in-memory journal and on Redis; Postgres and Cassandra profiles are config-only and untested here |
| **Clojure** (first‑class) | Clojure | yes: `clojure` | online | per‑conv serialize | **Datomic** (immutable) | runner (EDN and YAML) | `agentic.conformance/run-all`, `datomic_test.clj` | A pure-Clojure reimplementation, not core-backed; C2 is a per-conversation convention, not an engine partition. The cold tier is exact cosine KNN, not HNSW |
| **Local** (in‑proc) | all 3 | yes for Python (`python`) and Java (`jvm-core`, `python-jvm`); no for Go | online | per‑key lock | in‑mem/Redis | runner (all 3) | `ConformanceTest`, `matrix_binding`, core suites | Go runs the banking goldens and its own tests only |
| **Kafka Streams** | JVM | no | streamed | partition | changelog store | engine Runtime | `TopologyTestDriver` (2 tests) | `processing.guarantee` is never set in `BankingTopology`, so exactly-once is a design note in [`kafka-streams.md`](kafka-streams.md), not shipped behaviour. LLM calls on the stream thread must be split across a response topic |
| **Temporal** | JVM/Go | no | online | one exec/id | event‑sourced | engine Runtime | Temporal test suite (2 tests) | Runs against the Temporal test server only |
| **Pulsar Functions** | JVM | no | streamed | Key_Shared | BookKeeper state | engine Runtime | in‑memory context (2 tests) | Never run against a Pulsar broker here. LLM calls on the function thread must be split |
| **NATS JetStream** | Py/Go | no | online | subject+CAS | KV + stream | runner (`--backend nats`) | live JetStream round trip when a server is up | Single-writer is a convention (subject routing plus KV compare-and-set), not a partition guarantee. Needs `nats-py`, `ports/experimental/nats` on `PYTHONPATH`, and a server; without one the command fails with `ConnectionRefusedError` |
| **Celery** | Python | no | online | queue+lock | acks+store | runner (`--backend celery`) | live Celery task when a broker is up | State lives in an external store |
| **Faust** | Python | no | streamed | Kafka partition | Kafka changelog | core build | import check only | Cannot run on the Python used here (no wheel for 3.14); engine-guarded. LLM calls on the stream thread must be split |
| **Ray** | Python | no | online/actor | actor | external | core build | import check only | Same wheel problem as Faust. Durability is write-through to an external store |
| **Quarkus** | JVM | no | streamed | partition | external store | engine Runtime | compiles, 0 tests | |
| **Spring** | JVM | no | online/msg | partition | external store | engine Runtime | compiles, 0 tests | |
| **Dask** | Python | no | batch |, | retry | core build | runs live | Batch only: hosts parallel RAG ingest and offline eval, not the live turn loop. No C2 |
| **Airflow** | Python | no | batch/sched |, | retry/idempotent | core build | `simulate()` only | Batch only: DAG branching for scheduled work. No C2, never run on a scheduler here |

### Backend notes

The per-row caveats are in the table above. Two backends need more than a cell:

- **Clojure** is the one backend that is *not* core-backed: it's a **pure-Clojure
  reimplementation** of the essence (no `jagentic-core` dep), kept at parity by the same FNV +
  banking goldens. Single-writer (C2) is a per-conversation convention; durability (C3) is
  **Datomic**: immutable datoms give the conversation log + time-travel natively, on the in-process
  `com.datomic/local` **or an external Datomic Pro / Cloud** (same client API, selected by config). The
  Clojure loader runs the full shared schema, `banking.yaml`, `banking-llm.yaml` and
  `banking-rag.yaml` (skills, context-window, classifier guardrail), with one nuance: its cold tier
  is **exact cosine KNN** (a correctness-superset of HNSW ANN), not an approximate index. It ships the
  full stream-stateful core (`stream`/`timers`/`windows`/`cep`/`replay`/`suspend`/`trace`), the
  declarative `cep:` weave, formal Embedder/Classifier SPIs, **and an MCP client + A2A client** (with
  `mcp:`/`a2a:` wired into the loader), now at full parity with the other three cores. See
  [`clojure.md`](clojure.md) and [`../../agentic-clj/`](../../agentic-clj/).
- **Pekko** runs the same specs via `backend: pekko`; the `PipelineMain` CLI drives any `pipeline.yaml`
  through the event-sourced actor runtime (`banking`, `banking-llm`, `banking-rag` all covered by
  `PekkoBackendPipelineTest`).

## 3. What stays Flink-first (by design)

These are genuinely runtime-native and not meaningfully portable; reimplementing them on
Airflow/Celery would be pointless, so they remain first-class-Flink-only:

- **event-time CEP with watermarks**: the cores now ship a **portable CEP NFA matcher**
  (`cep`, processing/logical-time, the same `begin/next/followedBy/within` patterns), woven into the
  declarative pipeline via a **`cep:` section** (matches fire `tool`/`submit` actions) on every core;
  the *same* `cep:` spec also translates to **native Flink CEP** (`CepSpecTranslator`). So complex
  event processing is no longer Flink-only, what stays Flink-native is **event-time** matching with
  watermarks + out-of-order handling over a distributed stream;
- **HNSW vector memory backed by *Flink state*** (checkpointed/keyed), note the cores now
  ship their own in-process HNSW index (`HnswVectorStore`), so approximate-nearest-neighbour
  search itself is portable; what stays Flink-only is binding that index to Flink's managed,
  checkpointed state;
- **Flink checkpoint/savepoint** exactly-once semantics.

Every other agentic capability is implemented in the cores and reachable from every
adapter; whether a given runtime has proven it is a question for
[`../capabilities.md`](../capabilities.md), not this page.

## 4. How to read this for a decision

Pick by your delivery need first (online turn? keyed stream? batch?), then by the
durability/ordering you want native vs external, then by your stack. The
**[choosing-a-backend guide](choosing-a-backend.md)** walks that decision; the
**[pipelines guide](pipelines.md)** shows the one YAML that runs on whichever you pick.
